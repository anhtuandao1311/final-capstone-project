const path = {
  home: '/',
  profile: '/profile',
  login: '/login',
  register: '/register',
  logout: '/logout',
  courseDetails: '/courseDetails',
  search: '/search'
} as const

export default path
