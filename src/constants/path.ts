const path = {
  home: '/',
  profile: '/profile',
  login: '/login',
  register: '/register',
  logout: '/logout',
  courseDetails: '/courseDetails',
  search: '/search',
  dashboard: '/dashboard',
  authentication: '/authentication',
} as const

export default path
